name: rgcn
channels:
    - conda-forge
    - defaults
dependencies:
    - dvc
    - ipython
    - pandas
    - pyarrow
    - prefect
    - scikit-learn
    - tensorflow >= 2.0
    - xarray
    - zarr
